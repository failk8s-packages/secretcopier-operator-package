apiVersion: packaging.carvel.dev/v1alpha1
kind: PackageInstall
metadata:
  name: secretcopier-operator-package
  namespace: secretcopier-operator-package
spec:
  serviceAccountName: secretcopier-operator-sa
  packageRef:
    refName: secretcopier-operator.tkgdev.failk8s.com
    versionSelection:
      constraints: 1.0.0
      prereleases: {}
  values:
  - secretRef:
      name: secretcopier-operator-config
